services:
  - type: web
    name: lise-robot
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn admin:app & python bot.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
